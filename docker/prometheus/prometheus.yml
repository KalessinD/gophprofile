global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'gophprofile-server'
    static_configs:
      # Server exposes metrics on the same port as the app (8080) 
      # after we integrate OpenTelemetry in Go code
      - targets: ['server:8080']
